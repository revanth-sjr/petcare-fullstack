'use client';import OwnerShell from '@/components/OwnerShell';import {useEffect,useState} from 'react';export default function Pets(){const[pets,setPets]=useState<any[]>([]);const[name,setName]=useState('');const load=()=>fetch('/api/pets').then(r=>r.json()).then(setPets);useEffect(()=>{load()},[]);async function add(){if(!name)return;await fetch('/api/pets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,type:'Dog'})});setName('');load()}return <OwnerShell><h2>My Pets</h2><div className="form"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Pet name"/><button className="btn" onClick={add}>+ Add Pet</button></div><div className="pet-grid section">{pets.map(p=><div className="pet" key={p._id}><h3>{p.avatar||'🐾'} {p.name}</h3><p>{p.breed||p.type||'Pet profile'}</p><span className="badge">Manage</span></div>)}</div></OwnerShell>}
