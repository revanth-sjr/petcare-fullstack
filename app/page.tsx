import {auth} from '@clerk/nextjs/server';import {redirect} from 'next/navigation';export default async function P(){const{userId,sessionClaims}=await auth();if(!userId)return <main className="landing"><h1>🐾 PetCare</h1><p>Everything your pet needs, in one place.</p><a href="/sign-in">Sign in</a></main>;const r=((sessionClaims as any)?.publicMetadata?.role)||'owner';redirect(r==='caretaker'?'/caretaker/dashboard':'/owner/dashboard')}
