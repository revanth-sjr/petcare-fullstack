import {NextResponse} from 'next/server';import {auth} from '@clerk/nextjs/server';import db from '@/lib/mongodb';import Activity from '@/models/Activity';export async function PATCH(_:Request,{params}:{params:Promise<{id:string}>}){const{userId}=await auth();const{id}=await params;await db();const a=await Activity.findOne({_id:id,caretakerId:userId});if(!a)return NextResponse.json({error:'Forbidden or not found'},{status:403});a.status='completed';a.completionTime=new Date();a.completedBy=userId;await a.save();return NextResponse.json(a)}
