import {Schema,model,models} from 'mongoose'; export default models.Activity||model('Activity',new Schema({petId:String,ownerId:String,caretakerId:String,activityType:{type:String,enum:['feeding','medication','walk','exercise','grooming','appointment','other']},title:String,scheduledDate:String,scheduledTime:String,status:{type:String,enum:['pending','completed','missed','overdue'],default:'pending'},completionTime:Date,completedBy:String,notes:String},{timestamps:true}));
